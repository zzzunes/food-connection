const INITIAL_FOOD_STATE = {
    all: [],
}

const foodReducer = (state = INITIAL_FOOD_STATE, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
}

export default foodReducer;
